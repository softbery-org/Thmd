<UserControl x:Class="Thmd.Controls.VlcPlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Thmd.Controls"
             xmlns:vlc="clr-namespace:LibVLCSharp.WPF;assembly=LibVLCSharp.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <vlc:VideoView x:Name="_videoView" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="Black">
        <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <local:InfoBox x:Name="_infoBox" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,80,20,0" Width="auto" Height="auto"/>
            <local:SubtitleControl x:Name="_subtitleControl" VerticalAlignment="Top" Margin="0,280,0,0"  Height="150"/>
            <local:PlaylistView x:Name="_playlist" Width="400" Height="250" VerticalAlignment="Top" HorizontalAlignment="Left"/>
            <local:ControlBar x:Name="_controlBar" HorizontalAlignment="Left" VerticalAlignment="Top"/>
            <local:ProgressBarView x:Name="_progressBar" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" ProgressText="00:00:00"/>
            <local:LogoImage x:Name="_userImage" HorizontalAlignment="Right" VerticalAlignment="Top" Width="150" Height="75"  Margin="0,0,50,50"/>
            <!--<local:AddStreamView x:Name="_addStreamView" HorizontalAlignment="Center" VerticalContentAlignment="Center"/>-->
        </Grid>
    </vlc:VideoView>
</UserControl>