<ListView x:Class="Thmd.Controls.PlaylistView"
          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
          xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
          xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
          xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
          mc:Ignorable="d" 
          xmlns:local="clr-namespace:Thmd.Controls"
          xmlns:helper="clr-namespace:Thmd.Utilities"
          ItemsSource="{Binding Videos, RelativeSource={RelativeSource Self}}"
          Margin="0,25,0,0"
          Background="#2E2E2E"
          AllowDrop="True"
          VirtualizingStackPanel.IsVirtualizing="True"
          VirtualizingStackPanel.VirtualizationMode="Recycling" 
          d:ItemsSource="{d:SampleData ItemCount=5}">
    <FrameworkElement.Resources>
        <ResourceDictionary>
            <!-- Toolbar line -->
            <Style x:Key="ToolBarButtonStyle" TargetType="{x:Type Button}">
                <Setter Property="Background">
                    <Setter.Value>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Color="#D35400" Offset="0"/>
                            <GradientStop Color="#FF8C2F" Offset="1"/>
                        </LinearGradientBrush>
                    </Setter.Value>
                </Setter>
                <Setter Property="Foreground" Value="#E0E0E0"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="FontWeight" Value="Medium"/>
                <Setter Property="Padding" Value="10,5"/>
                <Setter Property="Margin" Value="5,0"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="BorderBrush">
                    <Setter.Value>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Color="#B34700" Offset="0"/>
                            <GradientStop Color="#E07B27" Offset="1"/>
                        </LinearGradientBrush>
                    </Setter.Value>
                </Setter>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="4">
                                <Border.Effect>
                                    <DropShadowEffect ShadowDepth="2" BlurRadius="4" Opacity="0.3"/>
                                </Border.Effect>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                <GradientStop Color="#FF6200" Offset="0"/>
                                                <GradientStop Color="#E65100" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                <GradientStop Color="#FF4500" Offset="0"/>
                                                <GradientStop Color="#D35400" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="#555555"/>
                                    <Setter Property="Foreground" Value="#888888"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <!-- ScrollViewer Style -->
            <Style x:Key="{x:Type ScrollViewer}" TargetType="{x:Type ScrollViewer}">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ScrollViewer}">
                            <Grid x:Name="Grid" Background="{TemplateBinding Background}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <ScrollContentPresenter x:Name="PART_ScrollContentPresenter"
                                                           Grid.Column="0"
                                                           Grid.Row="0"
                                                           Content="{TemplateBinding Content}"
                                                           ContentTemplate="{TemplateBinding ContentTemplate}"
                                                           CanContentScroll="{TemplateBinding CanContentScroll}"/>
                                <ScrollBar x:Name="PART_VerticalScrollBar"
                                               Grid.Column="1"
                                               Grid.Row="0"
                                               Orientation="Vertical"
                                               Maximum="{TemplateBinding ScrollableHeight}"
                                               ViewportSize="{TemplateBinding ViewportHeight}"
                                               Value="{TemplateBinding VerticalOffset}"
                                               Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}">
                                    <ScrollBar.Template>
                                        <ControlTemplate TargetType="{x:Type ScrollBar}">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="18"/>
                                                    <RowDefinition Height="*"/>
                                                    <RowDefinition Height="18"/>
                                                </Grid.RowDefinitions>
                                                <Border Grid.Row="1" Background="#3A3A3A" BorderThickness="1" CornerRadius="4">
                                                    <Border.BorderBrush>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                            <GradientStop Color="#D35400" Offset="0"/>
                                                            <GradientStop Color="#FF8C2F" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </Border.BorderBrush>
                                                </Border>
                                                <Track Grid.Row="1" x:Name="PART_Track" IsDirectionReversed="True">
                                                    <Track.Thumb>
                                                        <Thumb MinWidth="14" MinHeight="24">
                                                            <Thumb.Template>
                                                                <ControlTemplate TargetType="{x:Type Thumb}">
                                                                    <Border CornerRadius="4" Padding="3" BorderThickness="1">
                                                                        <Border.Background>
                                                                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                                                <GradientStop Color="#FF8C2F" Offset="0"/>
                                                                                <GradientStop Color="#D35400" Offset="1"/>
                                                                            </LinearGradientBrush>
                                                                        </Border.Background>
                                                                        <Border.BorderBrush>
                                                                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                                                <GradientStop Color="#B34700" Offset="0"/>
                                                                                <GradientStop Color="#E07B27" Offset="1"/>
                                                                            </LinearGradientBrush>
                                                                        </Border.BorderBrush>
                                                                        <Border.Effect>
                                                                            <DropShadowEffect ShadowDepth="2" BlurRadius="4" Opacity="0.3"/>
                                                                        </Border.Effect>
                                                                    </Border>
                                                                </ControlTemplate>
                                                            </Thumb.Template>
                                                        </Thumb>
                                                    </Track.Thumb>
                                                </Track>
                                                <RepeatButton Grid.Row="0" 
                                                                  Command="ScrollBar.LineUpCommand" 
                                                                  Content="↑" FontSize="10">
                                                    <RepeatButton.Background>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                            <GradientStop Color="#D35400" Offset="0"/>
                                                            <GradientStop Color="#FF8C2F" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </RepeatButton.Background>
                                                </RepeatButton>
                                                <RepeatButton Grid.Row="2" 
                                                                  Command="ScrollBar.LineDownCommand" 
                                                                  Content="↓" FontSize="10">
                                                    <RepeatButton.Background>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                            <GradientStop Color="#D35400" Offset="0"/>
                                                            <GradientStop Color="#FF8C2F" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </RepeatButton.Background>
                                                </RepeatButton>
                                            </Grid>
                                        </ControlTemplate>
                                    </ScrollBar.Template>
                                </ScrollBar>
                                <ScrollBar x:Name="PART_HorizontalScrollBar"
                                               Grid.Column="0"
                                               Grid.Row="1"
                                               Orientation="Horizontal"
                                               Maximum="{TemplateBinding ScrollableWidth}"
                                               ViewportSize="{TemplateBinding ViewportWidth}"
                                               Value="{TemplateBinding HorizontalOffset}"
                                               Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}">
                                    <ScrollBar.Template>
                                        <ControlTemplate TargetType="{x:Type ScrollBar}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="18"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="18"/>
                                                </Grid.ColumnDefinitions>
                                                <Border Grid.Column="1" Background="#3A3A3A" BorderThickness="1" CornerRadius="4">
                                                    <Border.BorderBrush>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                            <GradientStop Color="#D35400" Offset="0"/>
                                                            <GradientStop Color="#FF8C2F" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </Border.BorderBrush>
                                                </Border>
                                                <Track Grid.Column="1" x:Name="PART_Track">
                                                    <Track.Thumb>
                                                        <Thumb MinWidth="24" MinHeight="14">
                                                            <Thumb.Template>
                                                                <ControlTemplate TargetType="{x:Type Thumb}">
                                                                    <Border CornerRadius="4" Padding="3" BorderThickness="1">
                                                                        <Border.Background>
                                                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                                                <GradientStop Color="#FF8C2F" Offset="0"/>
                                                                                <GradientStop Color="#D35400" Offset="1"/>
                                                                            </LinearGradientBrush>
                                                                        </Border.Background>
                                                                        <Border.BorderBrush>
                                                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                                                <GradientStop Color="#B34700" Offset="0"/>
                                                                                <GradientStop Color="#E07B27" Offset="1"/>
                                                                            </LinearGradientBrush>
                                                                        </Border.BorderBrush>
                                                                        <Border.Effect>
                                                                            <DropShadowEffect ShadowDepth="2" BlurRadius="4" Opacity="0.3"/>
                                                                        </Border.Effect>
                                                                    </Border>
                                                                </ControlTemplate>
                                                            </Thumb.Template>
                                                        </Thumb>
                                                    </Track.Thumb>
                                                </Track>
                                                <RepeatButton Grid.Column="0" 
                                                                  Command="ScrollBar.LineLeftCommand" 
                                                                  Content="←" FontSize="10">
                                                    <RepeatButton.Background>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                            <GradientStop Color="#D35400" Offset="0"/>
                                                            <GradientStop Color="#FF8C2F" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </RepeatButton.Background>
                                                </RepeatButton>
                                                <RepeatButton Grid.Column="2" 
                                                                  Command="ScrollBar.LineRightCommand" 
                                                                  Content="→" FontSize="10">
                                                    <RepeatButton.Background>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                            <GradientStop Color="#D35400" Offset="0"/>
                                                            <GradientStop Color="#FF8C2F" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </RepeatButton.Background>
                                                </RepeatButton>
                                            </Grid>
                                        </ControlTemplate>
                                    </ScrollBar.Template>
                                </ScrollBar>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <!-- ListViewItem Style -->
            <Style TargetType="{x:Type ListViewItem}">
                <Setter Property="Background" Value="#424242"/>
                <Setter Property="Margin" Value="2"/>
                <Setter Property="Padding" Value="5"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="BorderBrush" Value="#555555"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ListViewItem}">
                            <Border x:Name="Border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="4">
                                <Border.Effect>
                                    <DropShadowEffect ShadowDepth="2" BlurRadius="4" Opacity="0.2"/>
                                </Border.Effect>
                                <ContentPresenter Margin="{TemplateBinding Padding}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#FF6200"/>
                                    <Setter Property="BorderBrush" Value="#FF8C2F"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#E65100"/>
                                    <Setter Property="BorderBrush" Value="#FF8C2F"/>
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsSelected" Value="True"/>
                                        <Condition Property="IsMouseOver" Value="True"/>
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="#FF4500"/>
                                    <Setter Property="BorderBrush" Value="#FF8C2F"/>
                                </MultiTrigger>
                                <!-- Nowy Trigger dla DragOver -->
                                <Trigger Property="helper:DragDropHelper.IsDragOver" Value="True">
                                    <Setter TargetName="Border" Property="BorderBrush" Value="#FFD700"/>
                                    <Setter TargetName="Border" Property="BorderThickness" Value="2"/>
                                    <Setter TargetName="Border" Property="Background" Value="#4A2E00"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </FrameworkElement.Resources>

    <ListView.Template>
        <ControlTemplate TargetType="{x:Type ListView}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <DockPanel Grid.Row="0" Grid.ColumnSpan="4" Background="#2E2E2E" Margin="0,0,0,0">
                    <Button Content="Dodaj" Command="{Binding AddCommand, RelativeSource={RelativeSource AncestorType={x:Type local:PlaylistView}}}" Style="{StaticResource ToolBarButtonStyle}"/>
                    <Button Content="Usuń" Command="{Binding RemoveCommand, RelativeSource={RelativeSource AncestorType={x:Type local:PlaylistView}}}" Style="{StaticResource ToolBarButtonStyle}"/>
                    <Button Content="Edytuj" Command="{Binding EditCommand, RelativeSource={RelativeSource AncestorType={x:Type local:PlaylistView}}}" Style="{StaticResource ToolBarButtonStyle}"/>
                    <Button Content="X" Command="{Binding CloseCommand, RelativeSource={RelativeSource AncestorType={x:Type local:PlaylistView}}}" Style="{StaticResource ToolBarButtonStyle}"/>
                    <StackPanel/>
                </DockPanel>

                <Border Grid.Row="1" Grid.ColumnSpan="4" Grid.Column="0" BorderThickness="3" BorderBrush="{TemplateBinding Background}">
                        <ScrollViewer Style="{StaticResource {x:Type ScrollViewer}}">
                            <ItemsPresenter/>
                        </ScrollViewer>
                </Border>
            </Grid>
        </ControlTemplate>
    </ListView.Template>
    
    <ItemsControl.ItemTemplate>
        <DataTemplate>
            <Grid Margin="5">
                <behaviors:Interaction.Behaviors>
                    <behaviors:MouseDragElementBehavior ConstrainToParentBounds="True"/>
                </behaviors:Interaction.Behaviors>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="350"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Padding="8" CornerRadius="4">
                    <FrameworkElement.Style>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="BorderBrush" Value="#FF8C2F"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Style.Triggers>
                                <DataTrigger Value="True" Binding="{Binding Path=IsSelected, RelativeSource={RelativeSource AncestorType=ListViewItem}}">
                                    <Setter Property="BorderBrush" Value="#FF4500"/>
                                    <Setter Property="Background" Value="#4A2E00"/>
                                </DataTrigger>
                                <DataTrigger Value="True" Binding="{Binding Path=IsMouseOver, RelativeSource={RelativeSource AncestorType=ListViewItem}}">
                                    <Setter Property="Background" Value="#3C2500"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </FrameworkElement.Style>
                    <TextBlock FontWeight="Medium" 
                                   TextWrapping="Wrap" 
                                   Text="{Binding Name}" 
                                   FontSize="14" 
                                   Foreground="#E0E0E0"/>
                </Border>
                <Border Grid.Column="1" BorderThickness="2,0,0,0" Padding="8" CornerRadius="4">
                    <FrameworkElement.Style>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="BorderBrush" Value="#FF8C2F"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Style.Triggers>
                                <DataTrigger Value="True" Binding="{Binding Path=IsSelected, RelativeSource={RelativeSource AncestorType=ListViewItem}}">
                                    <Setter Property="BorderBrush" Value="#FF4500"/>
                                    <Setter Property="Background" Value="#4A2E00"/>
                                </DataTrigger>
                                <DataTrigger Value="True" Binding="{Binding Path=IsMouseOver, RelativeSource={RelativeSource AncestorType=ListViewItem}}">
                                    <Setter Property="Background" Value="#3C2500"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </FrameworkElement.Style>
                    <Grid>
                        <TextBlock FontWeight="Medium" 
                                       Text="{Binding PositionFormatted}" 
                                       FontSize="14" 
                                       Foreground="#E0E0E0" 
                                       HorizontalAlignment="Center"/>
                    </Grid>
                </Border>
                <Border Grid.Column="2" BorderThickness="2,0,0,0" Padding="8" CornerRadius="4">
                    <FrameworkElement.Style>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="BorderBrush" Value="#FF8C2F"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Style.Triggers>
                                <DataTrigger Value="True" Binding="{Binding Path=IsSelected, RelativeSource={RelativeSource AncestorType=ListViewItem}}">
                                    <Setter Property="BorderBrush" Value="#FF4500"/>
                                    <Setter Property="Background" Value="#4A2E00"/>
                                </DataTrigger>
                                <DataTrigger Value="True" Binding="{Binding Path=IsMouseOver, RelativeSource={RelativeSource AncestorType=ListViewItem}}">
                                    <Setter Property="Background" Value="#3C2500"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </FrameworkElement.Style>
                    <StackPanel>
                        <TextBlock FontWeight="Bold" 
                                       Foreground="#FF4500" 
                                       Text="{Binding Duration}" 
                                       FontSize="14"/>
                        <TextBlock Text="{Binding Fps}" 
                                       FontSize="12" 
                                       Foreground="#B0B0B0"/>
                        <TextBlock Text="{Binding FrameSize}" 
                                       FontSize="12" 
                                       Foreground="#B0B0B0"/>
                        <TextBlock Text="{Binding Format}" 
                                       FontSize="12" 
                                       Foreground="#B0B0B0"/>
                    </StackPanel>
                </Border>
            </Grid>
        </DataTemplate>

    </ItemsControl.ItemTemplate>
</ListView>