<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="Thmd.Controls.ControlButtons.PlayerVolumeButtonControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Thmd.Controls.ControlButtons"
             xmlns:Controls="clr-namespace:Thmd.Controls">
  <FrameworkElement.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Thmd;component/Templates/PlayerSecondButtons.xaml" />
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </FrameworkElement.Resources>
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="50" />
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="50" />
      <ColumnDefinition Width="50" />
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="20" />
      <RowDefinition Height="*" />
      <RowDefinition Height="20" />
    </Grid.RowDefinitions>
    <Rectangle Grid.Column="0" Grid.ColumnSpan="4" Fill="#123341" Stroke="#000000" StrokeThickness="3" RadiusX="30" RadiusY="30" Grid.RowSpan="3" />
    <Button Name="_btnVolumeDown" Style="{StaticResource VolumeDown}" HorizontalAlignment="Center" Margin="0,5,0,0" Grid.RowSpan="3" />
    <Controls:ProgressBarControl x:Name="_volumeProgressBar"  HorizontalAlignment="Stretch" VerticalAlignment="Center" Grid.Row="1" Grid.Column="1" />
    <Button Name="_btnVolumeUp" Style="{StaticResource VolumeUp}" Grid.RowSpan="3" Grid.Column="2" />
    <Button Name="_btnMute" Style="{StaticResource Mute}" Grid.RowSpan="3" Grid.Column="3" />
  </Grid>
</UserControl>