<UserControl x:Class="Thmd.Controls.ProgressBarBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Thmd.Controls"
             Height="Auto"
             Width="Auto">
    <UserControl.Resources>
        <ResourceDictionary>
            <!-- Animacje -->
            <Storyboard x:Key="fadeOutProgressBar" FillBehavior="HoldEnd">
                <DoubleAnimation Storyboard.TargetName="_grid" 
                                Storyboard.TargetProperty="Opacity" 
                                To="0.0" 
                                Duration="0:0:3"/>
            </Storyboard>
            <Storyboard x:Key="fadeInProgressBar" FillBehavior="HoldEnd">
                <DoubleAnimation Storyboard.TargetName="_grid" 
                                Storyboard.TargetProperty="Opacity" 
                                To="1.0" 
                                Duration="0:0:2"/>
            </Storyboard>

            <!-- Styl dla ProgressBar -->
            <Style TargetType="{x:Type ProgressBar}">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ProgressBar}">
                            <Grid>
                                <Border x:Name="PART_Track" 
                                        Background="#3A3A3A" 
                                        CornerRadius="4" 
                                        BorderThickness="1">
                                    <Border.BorderBrush>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#D35400" Offset="0"/>
                                            <GradientStop Color="#FF8C2F" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.BorderBrush>
                                    <Border.Effect>
                                        <DropShadowEffect ShadowDepth="2" BlurRadius="4" Opacity="0.2"/>
                                    </Border.Effect>
                                </Border>
                                <Border x:Name="PART_Indicator" 
                                        HorizontalAlignment="Left" 
                                        Width="{TemplateBinding Value}" 
                                        CornerRadius="4">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#FF8C2F" Offset="0"/>
                                            <GradientStop Color="#D35400" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                    <Border.BorderBrush>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#B34700" Offset="0"/>
                                            <GradientStop Color="#E07B27" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.BorderBrush>
                                    <Border BorderThickness="1"/>
                                </Border>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Name="_grid" 
          Background="#2E2E2E" 
          Margin="0" 
          MinHeight="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!-- Bufor -->
        <Rectangle Name="_rectangleBufferMedia" 
                   HorizontalAlignment="Left" 
                   Width="12" 
                   Height="8" 
                   Margin="0,0,0,2"
                   VerticalAlignment="Center">
            <Rectangle.Fill>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#72DEDEDE" Offset="0"/>
                    <GradientStop Color="#72B0B0B0" Offset="1"/>
                </LinearGradientBrush>
            </Rectangle.Fill>
            <Rectangle.Effect>
                <DropShadowEffect ShadowDepth="1" BlurRadius="3" Opacity="0.3"/>
            </Rectangle.Effect>
        </Rectangle>
        <!-- Pasek postępu -->
        <ProgressBar Name="_progressBar" 
                     VerticalAlignment="Center" 
                     Height="8" 
                     Value="70" 
                     SmallChange="0.01" 
                     BorderBrush="{x:Null}"/>
        <!-- Znacznik myszy -->
        <Rectangle Name="_rectangleMouseOverPoint" 
                   HorizontalAlignment="Left" 
                   Width="4" 
                   Height="12" 
                   Margin="100,0,0,0" 
                   VerticalAlignment="Center">
            <Rectangle.Fill>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#FF4500" Offset="0"/>
                    <GradientStop Color="#E65100" Offset="1"/>
                </LinearGradientBrush>
            </Rectangle.Fill>
            <Rectangle.Effect>
                <DropShadowEffect ShadowDepth="1" BlurRadius="3" Opacity="0.3"/>
            </Rectangle.Effect>
        </Rectangle>
        <!-- Tekst postępu -->
        <TextBlock VerticalAlignment="Center" 
                   HorizontalAlignment="Center" 
                   TextAlignment="Center" 
                   Foreground="#E0E0E0" 
                   FontSize="14" 
                   FontWeight="Medium" 
                   Text="{Binding ProgressText}">
            <UIElement.Effect>
                <DropShadowEffect Color="#FF8C2F" ShadowDepth="2" BlurRadius="4" Opacity="0.3"/>
            </UIElement.Effect>
        </TextBlock>
        <!-- Popup -->
        <Popup Name="_popup" 
               AllowsTransparency="True" 
               Placement="Top" 
               PlacementTarget="{Binding ElementName=_progressBar}" 
               PopupAnimation="Fade" 
               VerticalOffset="-40">
            <Border Background="#424242" 
                    BorderBrush="#FF8C2F" 
                    BorderThickness="1" 
                    CornerRadius="4" 
                    Padding="8">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="2" BlurRadius="4" Opacity="0.3"/>
                </Border.Effect>
                <TextBlock Name="_popuptext" 
                           Foreground="#E0E0E0" 
                           FontSize="14" 
                           FontWeight="Bold" 
                           Text="00:00:00" 
                           VerticalAlignment="Center" 
                           HorizontalAlignment="Center" 
                           TextAlignment="Center">
                    <UIElement.Effect>
                        <DropShadowEffect Color="#FF4500" ShadowDepth="1" BlurRadius="3" Opacity="0.3"/>
                    </UIElement.Effect>
                </TextBlock>
            </Border>
        </Popup>
    </Grid>
</UserControl>