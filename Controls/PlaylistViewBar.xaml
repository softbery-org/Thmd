<UserControl x:Class="Thmd.Controls.PlaylistViewBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Thmd.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="400" Width="400" Height="250">
    <FrameworkElement.Resources>
        <ResourceDictionary>
            <!-- Toolbar line -->
            <Style x:Key="ToolBarButtonStyle" TargetType="{x:Type Button}">
                <Setter Property="BorderBrush" Value="#555555"/>
                <Setter Property="Background">
                    <Setter.Value>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Color="#D35400" Offset="0"/>
                            <GradientStop Color="#FF8C2F" Offset="1"/>
                        </LinearGradientBrush>
                    </Setter.Value>
                </Setter>
                <Setter Property="Foreground" Value="#E0E0E0"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="FontWeight" Value="Medium"/>
                <Setter Property="Padding" Value="10,5"/>
                <Setter Property="Margin" Value="5,0"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="BorderBrush">
                    <Setter.Value>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Color="#B34700" Offset="0"/>
                            <GradientStop Color="#E07B27" Offset="1"/>
                        </LinearGradientBrush>
                    </Setter.Value>
                </Setter>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                                <Border.Effect>
                                    <DropShadowEffect ShadowDepth="2" BlurRadius="4" Opacity="0.3"/>
                                </Border.Effect>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                <GradientStop Color="#FF6200" Offset="0"/>
                                                <GradientStop Color="#E65100" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                <GradientStop Color="#FF4500" Offset="0"/>
                                                <GradientStop Color="#D35400" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="#555555"/>
                                    <Setter Property="Foreground" Value="#888888"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </FrameworkElement.Resources>
    <Grid Background="#3A3A3A">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <DockPanel Grid.Row="0" Grid.ColumnSpan="4" Background="#2E2E2E" VerticalAlignment="Center" HorizontalAlignment="Stretch">
            <Button Content="Add" Command="{Binding AddCommand, RelativeSource={RelativeSource AncestorType={x:Type local:PlaylistView}}}" Style="{StaticResource ToolBarButtonStyle}"/>
            <Button Content="Remove" Command="{Binding RemoveCommand, RelativeSource={RelativeSource AncestorType={x:Type local:PlaylistView}}}" Style="{StaticResource ToolBarButtonStyle}"/>
            <Button Content="Edit" Command="{Binding EditCommand, RelativeSource={RelativeSource AncestorType={x:Type local:PlaylistView}}}" Style="{StaticResource ToolBarButtonStyle}"/>
            <Button Content="Save" Command="{Binding SavePlaylistCommand, RelativeSource={RelativeSource AncestorType={x:Type local:PlaylistView}}}" Style="{StaticResource ToolBarButtonStyle}"/>
            <Button Content="Load" Command="{Binding LoadPlaylistCommand, RelativeSource={RelativeSource AncestorType={x:Type local:PlaylistView}}}" Style="{StaticResource ToolBarButtonStyle}"/>
            <StackPanel/>
            <Button Content="X" Command="{Binding CloseCommand, RelativeSource={RelativeSource AncestorType={x:Type local:PlaylistView}}}" Style="{StaticResource ToolBarButtonStyle}" HorizontalAlignment="Right"/>
        </DockPanel>
        <Border Grid.Row="1" Grid.ColumnSpan="4" BorderThickness="3" BorderBrush="#2E2E2E">
            <local:PlaylistView x:Name="_playlist"/>
        </Border>
    </Grid>
</UserControl>