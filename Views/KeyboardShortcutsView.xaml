<UserControl x:Class="Thmd.Views.KeyboardShortcutsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="#2E2E2E"
             MinWidth="420" MinHeight="500">
    
    <UserControl.Resources>
        <ResourceDictionary>
            
            <!-- Styl przycisku zamykającego (X) -->
            <Style x:Key="CloseButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="#E0E0E0"/>
                <Setter Property="FontSize" Value="18"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Padding" Value="10"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="15">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FF4500"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Styl wiersza skrótu -->
            <Style TargetType="Grid" x:Key="ShortcutRowStyle">
                <Setter Property="Margin" Value="0,4"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#3C2500"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- Styl klawisza (np. Space, →) -->
            <Style TargetType="Border" x:Key="KeyStyle">
                <Setter Property="Background">
                    <Setter.Value>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Color="#FF8C2F" Offset="0"/>
                            <GradientStop Color="#D35400" Offset="1"/>
                        </LinearGradientBrush>
                    </Setter.Value>
                </Setter>
                <Setter Property="CornerRadius" Value="6"/>
                <Setter Property="Padding" Value="10,6"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="BorderBrush">
                    <Setter.Value>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Color="#B34700" Offset="0"/>
                            <GradientStop Color="#E07B27" Offset="1"/>
                        </LinearGradientBrush>
                    </Setter.Value>
                </Setter>
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect ShadowDepth="2" BlurRadius="4" Opacity="0.4"/>
                    </Setter.Value>
                </Setter>
            </Style>

        </ResourceDictionary>
    </UserControl.Resources>

    <Border Background="#3A3A3A" CornerRadius="10" Margin="15">
        <Border.Effect>
            <DropShadowEffect Color="#FF8C2F" BlurRadius="15" ShadowDepth="0" Opacity="0.4"/>
        </Border.Effect>

        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Nagłówek -->
            <Grid Grid.Row="0" Margin="0,0,0,25">
                <TextBlock Text="Skróty klawiszowe"
                           FontSize="24"
                           FontWeight="SemiBold"
                           Foreground="#FF8C2F"
                           HorizontalAlignment="Center"/>
                <Button Content="✕"
                        Style="{StaticResource CloseButtonStyle}"
                        HorizontalAlignment="Right"
                        Command="{Binding CloseCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            </Grid>

            <!-- Lista skrótów -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel>

                    <!-- Odtwarzanie -->
                    <TextBlock Text="Odtwarzanie" FontSize="16" FontWeight="Medium" Foreground="#FF8C2F" Margin="0,15,0,8"/>

                    <Grid Style="{StaticResource ShortcutRowStyle}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Style="{StaticResource KeyStyle}">
                            <TextBlock Text="Space" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                        </Border>
                        <TextBlock Grid.Column="1" Text="Odtwarzaj / Pauza" Foreground="#E0E0E0" VerticalAlignment="Center" Margin="20,0"/>
                    </Grid>

                    <Grid Style="{StaticResource ShortcutRowStyle}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Style="{StaticResource KeyStyle}">
                            <TextBlock Text="→" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                        </Border>
                        <TextBlock Grid.Column="1" Text="Przewiń 5 sekund do przodu" Foreground="#E0E0E0" VerticalAlignment="Center" Margin="20,0"/>
                    </Grid>

                    <Grid Style="{StaticResource ShortcutRowStyle}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Style="{StaticResource KeyStyle}">
                            <TextBlock Text="←" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                        </Border>
                        <TextBlock Grid.Column="1" Text="Przewiń 5 sekund wstecz" Foreground="#E0E0E0" VerticalAlignment="Center" Margin="20,0"/>
                    </Grid>

                    <!-- Głośność -->
                    <TextBlock Text="Głośność" FontSize="16" FontWeight="Medium" Foreground="#FF8C2F" Margin="0,20,0,8"/>

                    <Grid Style="{StaticResource ShortcutRowStyle}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Style="{StaticResource KeyStyle}">
                            <TextBlock Text="↑" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                        </Border>
                        <TextBlock Grid.Column="1" Text="Głośniej (+5%)" Foreground="#E0E0E0" VerticalAlignment="Center" Margin="20,0"/>
                    </Grid>

                    <Grid Style="{StaticResource ShortcutRowStyle}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Style="{StaticResource KeyStyle}">
                            <TextBlock Text="↓" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                        </Border>
                        <TextBlock Grid.Column="1" Text="Ciszej (-5%)" Foreground="#E0E0E0" VerticalAlignment="Center" Margin="20,0"/>
                    </Grid>

                    <Grid Style="{StaticResource ShortcutRowStyle}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Style="{StaticResource KeyStyle}">
                            <TextBlock Text="M" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                        </Border>
                        <TextBlock Grid.Column="1" Text="Wycisz / Włącz dźwięk" Foreground="#E0E0E0" VerticalAlignment="Center" Margin="20,0"/>
                    </Grid>

                    <!-- Interfejs -->
                    <TextBlock Text="Interfejs" FontSize="16" FontWeight="Medium" Foreground="#FF8C2F" Margin="0,20,0,8"/>

                    <Grid Style="{StaticResource ShortcutRowStyle}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Style="{StaticResource KeyStyle}">
                            <TextBlock Text="F" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                        </Border>
                        <TextBlock Grid.Column="1" Text="Pełny ekran" Foreground="#E0E0E0" VerticalAlignment="Center" Margin="20,0"/>
                    </Grid>

                    <Grid Style="{StaticResource ShortcutRowStyle}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Style="{StaticResource KeyStyle}">
                            <TextBlock Text="Esc" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                        </Border>
                        <TextBlock Grid.Column="1" Text="Wyjdź z pełnego ekranu" Foreground="#E0E0E0" VerticalAlignment="Center" Margin="20,0"/>
                    </Grid>

                    <Grid Style="{StaticResource ShortcutRowStyle}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Style="{StaticResource KeyStyle}">
                            <TextBlock Text="P" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                        </Border>
                        <TextBlock Grid.Column="1" Text="Pokaż/Ukryj playlistę" Foreground="#E0E0E0" VerticalAlignment="Center" Margin="20,0"/>
                    </Grid>

                    <Grid Style="{StaticResource ShortcutRowStyle}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Style="{StaticResource KeyStyle}">
                            <TextBlock Text="S" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                        </Border>
                        <TextBlock Grid.Column="1" Text="Pokaż/Ukryj napisy" Foreground="#E0E0E0" VerticalAlignment="Center" Margin="20,0"/>
                    </Grid>

                    <!-- Nawigacja po liście -->
                    <TextBlock Text="Nawigacja" FontSize="16" FontWeight="Medium" Foreground="#FF8C2F" Margin="0,20,0,8"/>

                    <Grid Style="{StaticResource ShortcutRowStyle}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Style="{StaticResource KeyStyle}">
                            <TextBlock Text="N" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                        </Border>
                        <TextBlock Grid.Column="1" Text="Następny utwór" Foreground="#E0E0E0" VerticalAlignment="Center" Margin="20,0"/>
                    </Grid>

                    <Grid Style="{StaticResource ShortcutRowStyle}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Style="{StaticResource KeyStyle}">
                            <TextBlock Text="B" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                        </Border>
                        <TextBlock Grid.Column="1" Text="Poprzedni utwór" Foreground="#E0E0E0" VerticalAlignment="Center" Margin="20,0"/>
                    </Grid>

                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>