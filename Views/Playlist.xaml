<ListView x:Class="Thmd.Views.Playlist"
          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
          xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
          xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
          xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
          mc:Ignorable="d" 
          xmlns:local="clr-namespace:Thmd.Views"
          xmlns:helper="clr-namespace:Thmd.Utilities"
          ItemsSource="{Binding Videos}"
          Margin="0,25,0,0"
          Background="#2E2E2E"
          AllowDrop="True"
          ScrollViewer.CanContentScroll="True"
          VirtualizingStackPanel.IsVirtualizing="True"
          VirtualizingStackPanel.VirtualizationMode="Recycling" 
          d:ItemsSource="{d:SampleData ItemCount=5}"
          MinHeight="50" MinWidth="150">
    <FrameworkElement.Resources>
        <ResourceDictionary>
            <!-- Toolbar line -->
            <Style x:Key="ToolBarButtonStyle" TargetType="{x:Type Button}">
                <Setter Property="Background">
                    <Setter.Value>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Color="#D35400" Offset="0"/>
                            <GradientStop Color="#FF8C2F" Offset="1"/>
                        </LinearGradientBrush>
                    </Setter.Value>
                </Setter>
                <Setter Property="Foreground" Value="#E0E0E0"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="FontWeight" Value="Medium"/>
                <Setter Property="Padding" Value="10,5"/>
                <Setter Property="Margin" Value="5,0"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="BorderBrush">
                    <Setter.Value>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Color="#B34700" Offset="0"/>
                            <GradientStop Color="#E07B27" Offset="1"/>
                        </LinearGradientBrush>
                    </Setter.Value>
                </Setter>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4">
                                <Border.Effect>
                                    <DropShadowEffect ShadowDepth="2" BlurRadius="4" Opacity="0.3"/>
                                </Border.Effect>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                <GradientStop Color="#FF6200" Offset="0"/>
                                                <GradientStop Color="#E65100" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                <GradientStop Color="#FF4500" Offset="0"/>
                                                <GradientStop Color="#D35400" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="#555555"/>
                                    <Setter Property="Foreground" Value="#888888"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <!-- ScrollViewer Style -->
            <Style x:Key="{x:Type ScrollViewer}" TargetType="{x:Type ScrollViewer}">
                <Setter Property="CanContentScroll" Value="True"/>
                <!-- Fix: Jawne ustawienie w stylu -->
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ScrollViewer}">
                            <Grid x:Name="Grid" Background="{TemplateBinding Background}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <ScrollContentPresenter x:Name="PART_ScrollContentPresenter"
                                                        Grid.Column="0"
                                                        Grid.Row="0"
                                                        Content="{TemplateBinding Content}"
                                                        ContentTemplate="{TemplateBinding ContentTemplate}"
                                                        CanContentScroll="{TemplateBinding CanContentScroll}"/>
                                <ScrollBar x:Name="PART_VerticalScrollBar"
                                           Grid.Column="1"
                                           Grid.Row="0"
                                           Orientation="Vertical"
                                           Maximum="{TemplateBinding ScrollableHeight}"
                                           ViewportSize="{TemplateBinding ViewportHeight}"
                                           Value="{TemplateBinding VerticalOffset}"
                                           Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}">
                                    <ScrollBar.Template>
                                        <ControlTemplate TargetType="{x:Type ScrollBar}">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="18"/>
                                                    <RowDefinition Height="*"/>
                                                    <RowDefinition Height="18"/>
                                                </Grid.RowDefinitions>
                                                <Border Grid.Row="1" Background="#3A3A3A" BorderThickness="1" CornerRadius="4">
                                                    <Border.BorderBrush>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                            <GradientStop Color="#D35400" Offset="0"/>
                                                            <GradientStop Color="#FF8C2F" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </Border.BorderBrush>
                                                </Border>
                                                <Track Grid.Row="1" x:Name="PART_Track" IsDirectionReversed="True">
                                                    <Track.Thumb>
                                                        <Thumb MinWidth="14" MinHeight="24">
                                                            <Thumb.Template>
                                                                <ControlTemplate TargetType="{x:Type Thumb}">
                                                                    <Border CornerRadius="4" Padding="3" BorderThickness="1">
                                                                        <Border.Background>
                                                                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                                                <GradientStop Color="#FF8C2F" Offset="0"/>
                                                                                <GradientStop Color="#D35400" Offset="1"/>
                                                                            </LinearGradientBrush>
                                                                        </Border.Background>
                                                                        <Border.BorderBrush>
                                                                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                                                <GradientStop Color="#B34700" Offset="0"/>
                                                                                <GradientStop Color="#E07B27" Offset="1"/>
                                                                            </LinearGradientBrush>
                                                                        </Border.BorderBrush>
                                                                        <Border.Effect>
                                                                            <DropShadowEffect ShadowDepth="2" BlurRadius="4" Opacity="0.3"/>
                                                                        </Border.Effect>
                                                                    </Border>
                                                                </ControlTemplate>
                                                            </Thumb.Template>
                                                        </Thumb>
                                                    </Track.Thumb>
                                                </Track>
                                                <RepeatButton Grid.Row="0" 
                                                              Command="ScrollBar.LineUpCommand" 
                                                              Content="↑" FontSize="10">
                                                    <RepeatButton.Background>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                            <GradientStop Color="#D35400" Offset="0"/>
                                                            <GradientStop Color="#FF8C2F" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </RepeatButton.Background>
                                                </RepeatButton>
                                                <RepeatButton Grid.Row="2" 
                                                              Command="ScrollBar.LineDownCommand" 
                                                              Content="↓" FontSize="10">
                                                    <RepeatButton.Background>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                            <GradientStop Color="#D35400" Offset="0"/>
                                                            <GradientStop Color="#FF8C2F" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </RepeatButton.Background>
                                                </RepeatButton>
                                            </Grid>
                                        </ControlTemplate>
                                    </ScrollBar.Template>
                                </ScrollBar>
                                <ScrollBar x:Name="PART_HorizontalScrollBar"
                                           Grid.Column="0"
                                           Grid.Row="1"
                                           Orientation="Horizontal"
                                           Maximum="{TemplateBinding ScrollableWidth}"
                                           ViewportSize="{TemplateBinding ViewportWidth}"
                                           Value="{TemplateBinding HorizontalOffset}"
                                           Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}">
                                    <ScrollBar.Template>
                                        <ControlTemplate TargetType="{x:Type ScrollBar}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="18"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="18"/>
                                                </Grid.ColumnDefinitions>
                                                <Border Grid.Column="1" Background="#3A3A3A" BorderThickness="1" CornerRadius="4">
                                                    <Border.BorderBrush>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                            <GradientStop Color="#D35400" Offset="0"/>
                                                            <GradientStop Color="#FF8C2F" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </Border.BorderBrush>
                                                </Border>
                                                <Track Grid.Column="1" x:Name="PART_Track">
                                                    <Track.Thumb>
                                                        <Thumb MinWidth="24" MinHeight="14">
                                                            <Thumb.Template>
                                                                <ControlTemplate TargetType="{x:Type Thumb}">
                                                                    <Border CornerRadius="4" Padding="3" BorderThickness="1">
                                                                        <Border.Background>
                                                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                                                <GradientStop Color="#FF8C2F" Offset="0"/>
                                                                                <GradientStop Color="#D35400" Offset="1"/>
                                                                            </LinearGradientBrush>
                                                                        </Border.Background>
                                                                        <Border.BorderBrush>
                                                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                                                <GradientStop Color="#B34700" Offset="0"/>
                                                                                <GradientStop Color="#E07B27" Offset="1"/>
                                                                            </LinearGradientBrush>
                                                                        </Border.BorderBrush>
                                                                        <Border.Effect>
                                                                            <DropShadowEffect ShadowDepth="2" BlurRadius="4" Opacity="0.3"/>
                                                                        </Border.Effect>
                                                                    </Border>
                                                                </ControlTemplate>
                                                            </Thumb.Template>
                                                        </Thumb>
                                                    </Track.Thumb>
                                                </Track>
                                                <RepeatButton Grid.Column="0" 
                                                              Command="ScrollBar.LineLeftCommand" 
                                                              Content="←" FontSize="10">
                                                    <RepeatButton.Background>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                            <GradientStop Color="#D35400" Offset="0"/>
                                                            <GradientStop Color="#FF8C2F" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </RepeatButton.Background>
                                                </RepeatButton>
                                                <RepeatButton Grid.Column="2" 
                                                              Command="ScrollBar.LineRightCommand" 
                                                              Content="→" FontSize="10">
                                                    <RepeatButton.Background>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                            <GradientStop Color="#D35400" Offset="0"/>
                                                            <GradientStop Color="#FF8C2F" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </RepeatButton.Background>
                                                </RepeatButton>
                                            </Grid>
                                        </ControlTemplate>
                                    </ScrollBar.Template>
                                </ScrollBar>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            
            <!-- ListViewItem Style -->
            <Style TargetType="{x:Type ListViewItem}">
                <Setter Property="Background" Value="#424242"/>
                <Setter Property="Margin" Value="2"/>
                <Setter Property="Padding" Value="5"/>
                <Setter Property="BorderThickness" Value="3"/>
                <Setter Property="BorderBrush" Value="#555555"/>
                <Setter Property="helper:DragDropHelper.IsDragOver" Value="False"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ListViewItem}">
                            <Grid>
                                <!-- Główny element -->
                                <Border x:Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4">
                                    <Border.Effect>
                                        <DropShadowEffect ShadowDepth="2" BlurRadius="4" Opacity="0.2"/>
                                    </Border.Effect>
                                    <ContentPresenter Margin="{TemplateBinding Padding}"/>
                                </Border>

                                <!-- Linia docelowa (gdzie upuścimy) -->
                                <Rectangle x:Name="DropLine" Height="2" Fill="#FFD700" Opacity="0" Margin="0,0,0,-2" HorizontalAlignment="Stretch"/>
                            </Grid>

                            <ControlTemplate.Triggers>
                                <!-- Zaznaczony -->
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#FF6200"/>
                                    <Setter Property="BorderBrush" Value="#FF8C2F"/>
                                </Trigger>
                                <!-- Hover -->
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#E65100"/>
                                    <Setter Property="BorderBrush" Value="#FF8C2F"/>
                                </Trigger>
                                <!-- Drag-over (nad elementem) -->
                                <Trigger Property="helper:DragDropHelper.IsDragOver" Value="True">
                                    <Setter TargetName="Border" Property="BorderBrush" Value="#FFD700"/>
                                    <Setter TargetName="Border" Property="BorderThickness" Value="2"/>
                                    <Setter TargetName="Border" Property="Background" Value="#4A2E00"/>
                                    <Setter TargetName="DropLine" Property="Opacity" Value="1"/>
                                </Trigger>
                                <!-- Odtwarzany -->
                                <DataTrigger Binding="{Binding IsPlaying}" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="#5C2E00"/>
                                    <Setter TargetName="Border" Property="BorderBrush" Value="#FF8C2F"/>
                                    <Setter TargetName="Border" Property="BorderThickness" Value="3"/>
                                    <Setter TargetName="Border" Property="Effect">
                                        <Setter.Value>
                                            <DropShadowEffect BlurRadius="12" ShadowDepth="0" Color="#FF8C2F" Opacity="0.6"/>
                                        </Setter.Value>
                                    </Setter>
                                    <DataTrigger.EnterActions>
                                        <BeginStoryboard x:Name="PlayHighlightStoryboard">
                                            <Storyboard RepeatBehavior="Forever" AutoReverse="True">
                                                <ColorAnimation Storyboard.TargetName="Border"
                                                    Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                    From="#5C2E00" To="#803000" Duration="0:0:1.5"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </DataTrigger.EnterActions>
                                    <DataTrigger.ExitActions>
                                        <StopStoryboard BeginStoryboardName="PlayHighlightStoryboard"/>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="Border"
                                                                   Storyboard.TargetProperty="Effect">
                                                    <DiscreteObjectKeyFrame KeyTime="0:0:0">
                                                        <DiscreteObjectKeyFrame.Value>
                                                            <DropShadowEffect ShadowDepth="2" BlurRadius="4" Opacity="0.2"/>
                                                        </DiscreteObjectKeyFrame.Value>
                                                    </DiscreteObjectKeyFrame>
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </DataTrigger.ExitActions>
                                </DataTrigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            
            <!-- Zanikanie oraz pojawianie się kontroleki -->
            <Storyboard x:Key="fadeOutPlaylist" FillBehavior="HoldEnd">
                <DoubleAnimation Storyboard.TargetName="_grid"  Storyboard.TargetProperty="Opacity" To="0.0" Duration="0:0:2" />
            </Storyboard>
            <Storyboard x:Key="fadeInPlaylist" FillBehavior="HoldEnd">
                <DoubleAnimation Storyboard.TargetName="_grid"   Storyboard.TargetProperty="Opacity" To="1.0" Duration="0:0:4" />
            </Storyboard>
        </ResourceDictionary>
    </FrameworkElement.Resources>

    <ListView.Template>
        <ControlTemplate TargetType="{x:Type ListView}">
            <Grid x:Name="_grid" Background="#3A3A3A">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Toolbar -->
                <DockPanel Grid.Row="0" Grid.ColumnSpan="4" Background="#2E2E2E" VerticalAlignment="Center">
                    <Button Content="Add" Command="{Binding AddCommand, RelativeSource={RelativeSource AncestorType={x:Type local:Playlist}}}" Style="{StaticResource ToolBarButtonStyle}"/>
                    <Button Content="Remove" Command="{Binding RemoveCommand, RelativeSource={RelativeSource AncestorType={x:Type local:Playlist}}}" Style="{StaticResource ToolBarButtonStyle}"/>
                    <Button Content="Edit" Command="{Binding EditCommand, RelativeSource={RelativeSource AncestorType={x:Type local:Playlist}}}" Style="{StaticResource ToolBarButtonStyle}"/>
                    <Button Content="Save" Command="{Binding SavePlaylistCommand, RelativeSource={RelativeSource AncestorType={x:Type local:Playlist}}}" Style="{StaticResource ToolBarButtonStyle}"/>
                    <Button Content="Load" Command="{Binding LoadPlaylistCommand, RelativeSource={RelativeSource AncestorType={x:Type local:Playlist}}}" Style="{StaticResource ToolBarButtonStyle}"/>
                    <Button Content="Clear" Command="{Binding ClearPlaylistCommand, RelativeSource={RelativeSource AncestorType={x:Type local:Playlist}}}" Style="{StaticResource ToolBarButtonStyle}"/>
                    <StackPanel/>
                    <Button Content="X" Command="{Binding CloseCommand, RelativeSource={RelativeSource AncestorType={x:Type local:Playlist}}}" Style="{StaticResource ToolBarButtonStyle}" HorizontalAlignment="Right"/>
                </DockPanel>

                <!-- Lista z DropTarget -->
                <Border Grid.Row="1" Grid.ColumnSpan="4" BorderThickness="5" BorderBrush="{TemplateBinding Background}">
                    <ScrollViewer Style="{StaticResource {x:Type ScrollViewer}}" CanContentScroll="True">
                        <ItemsPresenter x:Name="ItemsPresenter"/>
                    </ScrollViewer>
                </Border>

                <!-- Podgląd przeciąganego elementu (ghost) -->
                <Canvas x:Name="DragAdorner" Visibility="Collapsed" IsHitTestVisible="False" Opacity="0.8">
                    <Border Background="#4A2E00" BorderBrush="#FFD700" BorderThickness="2" CornerRadius="6" Padding="8" MinWidth="300">
                        <Border.Effect>
                            <DropShadowEffect Color="#FF8C2F" BlurRadius="15" ShadowDepth="0" Opacity="0.7"/>
                        </Border.Effect>
                        <TextBlock Foreground="#FFD700" FontWeight="Bold" FontSize="14" Text="{Binding}"/>
                    </Border>
                </Canvas>
            </Grid>
        </ControlTemplate>
    </ListView.Template>

    <ItemsControl.ItemTemplate>
        <DataTemplate>
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="350"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Padding="8" CornerRadius="4">
                    <FrameworkElement.Style>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="BorderBrush" Value="#FF8C2F"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Style.Triggers>
                                <DataTrigger Value="True" Binding="{Binding Path=IsSelected, RelativeSource={RelativeSource AncestorType=ListViewItem}}">
                                    <Setter Property="BorderBrush" Value="#FF4500"/>
                                    <Setter Property="Background" Value="#4A2E00"/>
                                </DataTrigger>
                                <DataTrigger Value="True" Binding="{Binding Path=IsMouseOver, RelativeSource={RelativeSource AncestorType=ListViewItem}}">
                                    <Setter Property="Background" Value="#3C2500"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </FrameworkElement.Style>
                    <TextBlock FontWeight="Medium" 
                               TextWrapping="Wrap" 
                               Text="{Binding Name}" 
                               FontSize="14" 
                               Foreground="#E0E0E0"/>
                </Border>
                <Border Grid.Column="1" BorderThickness="2,0,0,0" Padding="8" CornerRadius="4">
                    <FrameworkElement.Style>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="BorderBrush" Value="#FF8C2F"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Style.Triggers>
                                <DataTrigger Value="True" Binding="{Binding Path=IsSelected, RelativeSource={RelativeSource AncestorType=ListViewItem}}">
                                    <Setter Property="BorderBrush" Value="#FF4500"/>
                                    <Setter Property="Background" Value="#4A2E00"/>
                                </DataTrigger>
                                <DataTrigger Value="True" Binding="{Binding Path=IsMouseOver, RelativeSource={RelativeSource AncestorType=ListViewItem}}">
                                    <Setter Property="Background" Value="#3C2500"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </FrameworkElement.Style>
                    <Grid>
                        <TextBlock FontWeight="Medium" 
                                   Text="{Binding PositionFormatted}" 
                                   FontSize="14" 
                                   Foreground="#E0E0E0" 
                                   HorizontalAlignment="Center"/>
                    </Grid>
                </Border>
                <Border Grid.Column="2" BorderThickness="2,0,0,0" Padding="8" CornerRadius="4">
                    <FrameworkElement.Style>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="BorderBrush" Value="#FF8C2F"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Style.Triggers>
                                <DataTrigger Value="True" Binding="{Binding Path=IsSelected, RelativeSource={RelativeSource AncestorType=ListViewItem}}">
                                    <Setter Property="BorderBrush" Value="#FF4500"/>
                                    <Setter Property="Background" Value="#4A2E00"/>
                                </DataTrigger>
                                <DataTrigger Value="True" Binding="{Binding Path=IsMouseOver, RelativeSource={RelativeSource AncestorType=ListViewItem}}">
                                    <Setter Property="Background" Value="#3C2500"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </FrameworkElement.Style>
                    <StackPanel>
                        <TextBlock FontWeight="Bold" 
                                   Foreground="#FF4500" 
                                   Text="{Binding Duration}" 
                                   FontSize="14"/>
                        <TextBlock Text="{Binding Fps}" 
                                   FontSize="12" 
                                   Foreground="#B0B0B0"/>
                        <TextBlock Text="{Binding FrameSize}" 
                                   FontSize="12" 
                                   Foreground="#B0B0B0"/>
                        <TextBlock Text="{Binding Format}" 
                                   FontSize="12" 
                                   Foreground="#B0B0B0"/>
                    </StackPanel>
                </Border>
            </Grid>
        </DataTemplate>
    </ItemsControl.ItemTemplate>
</ListView>