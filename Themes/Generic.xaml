<ResourceDictionary
    x:Class="Thmd.Themes.Generic"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Thmd.Converters">
    <!-- Custom Foreground Brush -->
    <SolidColorBrush x:Key="SystemControlForegroundBaseHighBrush" Color="White"/>
    <!-- Common Border Style -->
    <Style x:Key="CommonBorderStyle" TargetType="{x:Type Border}">
        <Setter Property="BorderBrush">
            <Setter.Value>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#D35400" Offset="0"/>
                    <GradientStop Color="#FF8C2F" Offset="1"/>
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="3"/>
        <Setter Property="Background">
            <Setter.Value>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#FF8C2F" Offset="0"/>
                    <GradientStop Color="#D35400" Offset="1"/>
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
    </Style>
    <!-- Common TextBlock Style -->
    <Style x:Key="CommonTextBlockStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{DynamicResource SystemControlForegroundBaseHighBrush}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="TextAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Opacity="0.3" ShadowDepth="1" Direction="315" Color="#535353"/>
            </Setter.Value>
        </Setter>
    </Style>
    <!-- Converter -->
    <local:PercentageToWidthConverter x:Key="PercentageToWidthConverter"/>
</ResourceDictionary>